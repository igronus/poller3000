temporal:
  targets:
    - host: "localhost"
      port: 7233
      namespace: "default"
    - host: "prod.temporal.io"
      port: 7233
      namespace: "production"

monitoring:
  poll_interval_seconds: 30
